# 진행 상황

마지막 갱신: 2026-07-30

## 지금 단계

**CSV/TSV 한 바퀴 완주.** 파일을 열면 차트가 그려진다.

인코딩 감지 → 파싱 → 컬럼 타입 추론 → 매핑 자동 채움 → 차트 7종 렌더링까지 이어진다.
추론이 틀리면 사이드바에서 고칠 수 있고, 고치면 매핑 후보와 차트가 따라 바뀐다.
모든 차트에 표 보기가 함께 있다.

CSV/TSV와 Excel(`.xlsx`/`.xls`) 모두 열리고, 헤더가 1행이 아닌 파일도 지정해서 읽는다.
남은 것은 좁은 폭 검증, 직접 라벨(최대·최신값) 정도다.

UI 셸과 데이터 로직을 일부러 분리해서 진행 중이다. 셸을 먼저 고정해두면 파서를 붙일 때
화면 구조를 다시 흔들 일이 없다.

## 화면 구조

```
┌─ AppHeader (h-14) ───────────────────────────────┐
│  워드마크          [파일 열기] │ [테마 토글]      │
├─ SettingsSidebar (w-72) ─┬─ main ────────────────┤
│  데이터셋                 │  ChartCanvas         │
│  컬럼 (타입 배지 + 수정)   │   empty   드롭존      │
│  차트 종류 (7종 피커)      │            ⋮         │
│  매핑 (종류별로 다름)      │   loading 스켈레톤     │
│                          │   error   Alert+드롭존 │
│  ─ 로컬 전용 계약 3줄 ─    │   ready   플롯 프레임  │
└──────────────────────────┴───────────────────────┘
```

`lg` 미만에서는 **캔버스가 위, 설정이 아래로** 쌓이고 페이지가 스크롤된다(`flex-col-reverse`).
설정이 위에 있으면 스크롤을 한참 내려야 차트가 나온다. 쌓였을 때는 사이드바 내용을
`max-w-lg`로 묶어 가운데 정렬한다 — 안 그러면 라벨과 Select가 양 끝으로 벌어진다.

### 높이는 백분율로 잡지 말 것

`flex-1`은 높이가 `auto`라서 **자식의 `h-full`(백분율)이 해석되지 못하고 콘텐츠 높이로
무너진다.** 실제로 쌓인 레이아웃에서 Recharts 플롯이 308px 자리에 47px로 그려졌다.
캔버스는 `relative` + 자식 `absolute inset-0`으로 채운다 — ChartView·DataTable 둘 다.

| 파일 | 역할 |
|---|---|
| `src/App.tsx` | 상태 보유(`CanvasState`, `ChartType`), 파일 핸들러 |
| `src/components/app-header.tsx` | 워드마크, 파일 열기, 테마 토글 |
| `src/components/settings-sidebar.tsx` | 데이터셋 카드 / 컬럼 / 차트 종류 / 매핑 / 로컬 전용 계약 |
| `src/components/chart-type-picker.tsx` | 7종 피커(막대/가로 막대/누적 막대/선/영역/산점도/원형) + 직접 그린 마크 글리프 |
| `src/components/chart-canvas.tsx` | 캔버스 4개 상태 |
| `src/components/file-dropzone.tsx` | 드래그&드롭 + 파일 선택 |
| `src/components/theme-toggle.tsx` | 라이트/다크 (`localStorage`, 데이터 아님) |
| `src/lib/file-constraints.ts` | 확장자 목록, 크기 상한(50MB), `validateFile()` |
| `src/components/column-list.tsx` | 컬럼별 타입 배지 + 덮어쓰기, 불확실 경고 |
| `src/lib/parse-file.ts` | 인코딩 감지 + papaparse 파싱, 행 상한(10만) |
| `src/lib/infer-types.ts` | 컬럼 타입 추론 (숫자/날짜/범주 + 신뢰도 + 고유값 수) |
| `src/lib/mapping-slots.ts` | 슬롯 정의, 후보 필터, 자동 채움, 무효 선택 정리 |
| `src/lib/aggregate.ts` | 집계(합계/평균/개수) + 차트가 먹을 모양으로 변환 |
| `src/components/chart-view.tsx` | Recharts 7종 렌더러 |
| `src/components/data-table.tsx` | 차트와 같은 집계 결과의 표 보기 |

## 파싱

`src/App.tsx`의 `handleFile()`이 흐름을 잡는다:

```
validateFile()  확장자·크기 (파싱 없이)
  → loading
  → parseFile()  전체 디코딩 → papaparse
  → 컬럼 0개 / 행 0개면 error
  → ready { dataset, data }
```

`ParsedFile`은 `columns` / `rows` / `encoding` / `truncated` / `errorCount`를 담는다.
`SettingsSidebar`는 `data.columns`로 매핑 Select를 채우고, 캔버스는 행·컬럼 수와 경고를
헤더에 띄운다.

### 왜 청크 스트리밍을 안 쓰는가

papaparse의 `chunk` 콜백은 청크마다 따로 디코딩해서 **10MB 경계에 걸친 멀티바이트 문자가
조용히 깨진다.** 37MB 한글 파일에서 2건 재현했다. 전체를 한 번에 디코딩하면 0건이다.

측정치(37MB, 6만 행): 디코딩 84ms + 파싱 102ms. 상한 50MB에서 200~300ms 메인스레드
블로킹을 감수한다. Worker로 옮겨도 결과 전달에 50ms가 들어서 실이익이 작다 — 더 큰
파일을 다뤄야 할 때 다시 볼 것.

### 헤더 행

CSV든 Excel이든 **행 배열(`string[][]`)로 읽은 뒤 `shape()`가 헤더를 뽑는다.** 제목 줄이
위에 붙은 리포트성 파일을 지원하려면 이 단계가 공통이어야 한다.

사이드바 데이터셋 섹션의 "헤더 행"에서 고른다. 목록은 번호만 아니라 **그 줄에 뭐가 들어
있는지**를 같이 보여준다(`4행 · 지역, 분기, 매출…`) — 번호만으로는 못 고른다. 앞
10행까지가 후보다.

행 번호는 **사용자가 파일에서 세는 줄 번호와 맞춘다.** 그래서 빈 줄을 그리드에 남기고
(papaparse `skipEmptyLines: false`, SheetJS `blankrows: true`), 데이터에서 빼는 건
`shape()`가 한다.

**그 대가로 구분자 추측이 깨진다.** papaparse는 빈 줄이 하나만 있어도(끝 개행 포함)
추측이 쉼표로 잘못 떨어져 TSV가 1컬럼이 된다. 그래서 빈 줄을 뺀 앞 64KB 샘플로 구분자를
먼저 정하고(`guessDelimiter`) 본 파싱에 명시적으로 넘긴다.

### Excel

`.xlsx`/`.xlsm`/`.xlsb`/`.xls`는 SheetJS로 간다. 확장자로 갈라서 papaparse에 넘기지 않는다 —
바이너리를 텍스트로 읽으면 **에러 없이 그럴듯한 가짜 데이터**가 나온다(랜덤 바이너리에서
"15행 2컬럼"이 나왔다).

SheetJS는 동적 import라 Excel을 열 때만 받는다(gzip 160KB 별도 청크). 메인 번들은
그대로다.

`raw: false`로 서식이 적용된 문자열을 받는다 — 통화·퍼센트는 분석가가 Excel에서 보던
모양 그대로 오는 편이 낫고, 숫자·날짜 판정은 우리 추론이 한다. **`dateNF`는
`sheet_to_json`이 아니라 `read`에 줘야 먹는다**(안 주면 셀 서식이 이겨서 `7/30/26`).
`raw: true`는 Date 객체를 주지만 시간대 때문에 하루가 밀린다.

시트가 여러 개면 사이드바 데이터셋 섹션에 시트 선택이 뜬다. 시트를 바꾸면 원본 파일을
다시 읽는다 — App이 `File` 참조를 들고 있다(데이터가 아니라 참조다). 큰 파일에서는
시트 전환이 그만큼 느리다.

또 챙긴 것: 중복 헤더는 `수량`, `수량 (2)`로 갈라주고(안 하면 조용히 덮어쓴다), 빈 헤더는
`열3`, 전부 빈 행은 버린다(Excel은 서식만 남은 빈 행을 흔히 들고 있다).

### 인코딩

앞 64KB를 UTF-8로 디코딩해보고 치환 문자가 나오면 EUC-KR로 다시 읽는다. 한국 Excel의
"CSV(쉼표로 분리)"가 CP949로 저장되기 때문이다. 감지 결과는 데이터셋 카드에 `EUC-KR`로
표시해서 감지가 틀렸을 때 사용자가 알아챌 수 있게 했다.

## 컬럼 타입 추론

전체에 걸쳐 띄엄띄엄 200개를 뽑아 세 종류로만 분류한다. 빈 값(`""`, `-`, `NA`, `null` …)은
세지 않는다. 샘플의 80% 이상이 맞아야 숫자·날짜라고 주장하고, 아니면 범주다.

날짜를 먼저 본다 — `20260730` 같은 값은 숫자로도 읽히기 때문이다. `new Date()`는 쓰지
않는다(`new Date("1")`도 통과한다). 알아보는 형태만:
`2026-07-30` `2026/7/5` `2026.7.5` `2026년 7월 30일` `20260730` `2026-07`

`ColumnInfo`는 `type`(현재) / `inferred`(추론 원본) / `confidence` / `sampled` /
`distinctCount`를 들고 있다. `type !== inferred`면 사용자가 손댄 것이고, 그때는 경고를 더
띄우지 않는다. 고유값은 50까지만 세고 멈춘다(`distinctCapped`) — 시리즈 후보 판정에는
그 위로 정확한 수가 필요 없고, 고유값이 많은 컬럼일수록 일찍 빠져나온다.

**의도적으로 이렇게 둔 것들**

| 값 | 추론 | 이유 |
|---|---|---|
| `06234`, `007` | 범주 | 선행 0이 붙은 정수는 우편번호·사번. 숫자로 보면 0이 날아간다 |
| `2026-Q2` | 범주 | 분기는 날짜가 아니라 순서 있는 구간 |
| `20260730` | 날짜 | 숫자로도 읽히지만 날짜 쪽이 더 그럴듯하다. 틀리면 사용자가 고친다 |
| `1,234,567`, `₩12,000`, `12.5%` | 숫자 | 천 단위 쉼표·통화 기호·퍼센트는 벗겨내고 본다 |

`06234`처럼 일부만 선행 0인 컬럼은 범주로 나오되 신뢰도가 낮게 잡혀 경고가 붙는다 —
이게 의도한 동작이다. 규칙으로 다 잡으려 하지 말고 사용자가 확인하게 한다.

## 차트 렌더링

집계는 `aggregate.ts`가 맡는다. 범주(또는 X축)로 묶고 합계/평균/개수 중 하나로 줄인다.
막대·원형은 큰 것부터, 선·영역은 X축 순서대로 정렬한다 — 추이 차트의 순서를 뒤섞으면
거짓말이 된다. 산점도만 집계하지 않는다(행 하나가 점 하나).

상한: 범주 30개(초과분은 "N개 생략"으로 알림), 원형 조각 6개(나머지는 "기타"로 접어야
합이 유지된다), 산점도 점 3000개.

### Recharts에서 걸린 것들

| 증상 | 원인 |
|---|---|
| 페이지가 세로로 스크롤됨 | `ChartContainer`의 기본 `aspect-video`. `aspect-auto`로 덮어야 한다 |
| 툴팁에 시리즈 이름이 안 나옴 | `formatter`를 주면 shadcn이 행 전체를 그걸로 대체한다. 안 주는 게 맞다 |
| 원형 조각 라벨이 조각 색으로 칠해짐 | Recharts가 물려준다. JSX 반환은 먹지 않으니 `[&_.recharts-pie-label-text]:fill-muted-foreground`로 덮는다 (CSS `fill`이 SVG 표현 속성보다 우선) |
| 산점도 점이 안 보임 | `<ZAxis range={[36,36]}>`가 반지름 0.9px로 뭉갠다. 빼면 기본값이 r≈4.5(지름 9px) |
| 플롯이 납작하게 눌림 | 부모가 `flex-1`(높이 auto)이면 `h-full`이 안 먹는다. `relative` + `absolute inset-0`으로 |
| Y축 라벨이 눈금과 겹침 | `insideLeft` 회전 라벨. 축 이름은 `AxisFrame`이 HTML로 그린다 |

### 규칙을 코드에 박아둔 곳

- 단일 시리즈는 `--chart-1` 하나 — 막대 길이가 이미 보여주는 값을 색으로 다시 칠하지 않는다
- 색은 시리즈 인덱스 고정 배정, 순환 없음. 9번째는 매핑 단계에서 이미 막혀 있다
- 누적 조각 사이는 카드 색 2px 틈(테두리 아님), 맨 위 조각만 끝이 둥글다
- 산점도 점은 카드 색 2px 링
- "기타"는 엔티티가 아니라서 무채색
- 표 보기는 모든 차트에 있다 — 라이트 모드에서 `--chart-3/4/5`가 대비 미달이라 색만으로
  값을 읽게 두면 안 되고, 직접 라벨을 다 붙이지 않아도 값에 닿을 수 있어야 한다

## 미결정 사항

- **헤더가 1행이 아닌 파일** — 제목 줄이 위에 붙은 리포트성 CSV·Excel. "헤더 행 지정" UI 필요.
- **큰 Excel의 시트 전환** — 지금은 원본을 다시 읽는다. 느려지면 워크북을 들고 있는 쪽으로.
- **상태관리** — 아직 `useState`로 충분. 매핑 선택과 파싱 결과가 들어오면 재검토

## 차트 종류별 매핑 슬롯

사이드바의 "매핑" 섹션은 선택된 차트 종류에 따라 라벨과 슬롯 개수가 바뀐다.
정의는 `src/lib/mapping-slots.ts`의 `MAPPING_SLOTS`에 있다.

| 종류 | 슬롯 |
|---|---|
| 막대 / 가로 막대 | 범주, 값, 분할*(선택)* |
| 누적 막대 | 범주, 값, 누적 기준 |
| 선 / 영역 / 산점도 | X축, Y축, 분할*(선택)* |
| 원형 | 범주, 값 |

선택 슬롯은 placeholder가 `없음`, 필수 슬롯은 `컬럼 선택`으로 나온다. 누적 막대의
누적 기준이 필수인 이유는 그게 없으면 그냥 막대이기 때문이다.

막대·원형은 역할 이름(범주/값)을, 선·산점도는 축 이름(X축/Y축)을 쓴다 — 후자는 양쪽이
모두 수치라서 역할로 부를 이름이 없다.

### 슬롯이 받는 타입

정의는 `src/lib/mapping-slots.ts`의 `MAPPING_SLOTS`. 후보에 없는 컬럼은 드롭다운에
아예 안 나오고, 후보가 하나도 없으면 왜 없는지를 placeholder에 적는다.

| 슬롯 | 받는 타입 |
|---|---|
| 값 / Y축 | 숫자 |
| 범주 | 범주, 날짜 |
| X축 (선·영역) | 숫자, 날짜, 범주 — 순서만 있으면 되므로 분기 같은 범주도 정당하다 |
| X축·Y축 (산점도) | 숫자 (관계를 보려면 양쪽이 수치여야 한다) |
| 분할 / 누적 기준 | 범주, 날짜 **+ 고유값 8개 이하** |

분할의 8개 상한은 색 슬롯이 8개라서다 — CLAUDE.md의 "9번째 시리즈는 색을 새로 만들지
말 것". 고유값이 많은 컬럼(고객명, 날짜 28종)은 자동으로 후보에서 빠진다.

**후보에서 빠졌는데 꼭 써야 한다면** 사이드바 "컬럼" 섹션에서 그 컬럼의 타입을 바꾸면
된다. 필터를 느슨하게 만드는 대신 이 탈출구를 열어뒀다.

### 자동 채움과 정리

- 파일을 열거나 차트 종류를 바꾸면 **비어 있는 필수 슬롯을 첫 후보로 채운다**(`fillMapping`).
  선택 슬롯(분할)은 건드리지 않는다 — 묻지도 않았는데 시리즈를 쪼개면 놀란다.
- 차트 종류나 컬럼 타입이 바뀌어 예전 선택이 더는 후보가 아니면 걷어낸다(`pruneMapping`).
  단, **지금 종류에 없는 슬롯의 값은 남겨둔다** — 그게 아래 "같은 key면 유지"의 근거다.

### 선택값은 슬롯 key로 살아남는다

선택 상태는 `App.tsx`가 `Mapping`(= `Partial<Record<MappingKey, string>>`)으로 들고 있다.
차트 종류를 바꿔도 **같은 key면 선택한 컬럼이 유지**된다.

```
막대   범주=region  값=—  분할=quarter
 → 선   X축=—  Y축=—  분할=quarter      (series만 넘어옴)
 → 원형  범주=region  값=—               (category가 되살아남)
 → 막대  범주=region  값=—  분할=quarter  (원래대로)
```

`category`와 `x`는 역할이 달라서 서로 넘어가지 않는다. 현재 종류에 없는 슬롯의 값도
버리지 않고 들고 있다가 되돌아오면 복원한다. 새 파일을 열면 컬럼 자체가 바뀌므로
`handleFile`에서 전부 비운다.

선택 슬롯(분할)은 목록 맨 위의 `없음` 항목으로 되돌릴 수 있다. Radix `SelectItem`이 빈
문자열 value를 못 받아서, `없음`은 `none`을 쓰고 **컬럼 쪽에 `col:` 접두사를 붙인다** —
이러면 `none`이라는 이름의 컬럼이 있어도 겹치지 않는다. 선택 슬롯은 비어 있을 때도
`none`이 실제로 선택된 상태라서 목록에 체크 표시가 남는다.

## 확인된 함정

- **Radix `TooltipTrigger asChild`로 `ToggleGroupItem`을 감싸지 말 것.** 툴팁 트리거의
  props가 나중에 spread되어 `data-state` / `value` / `aria-pressed`를 덮어쓴다. 선택 상태
  스타일과 라디오 시맨틱이 조용히 죽는다. 차트 종류 피커는 이 때문에 툴팁 대신 선택된
  종류의 설명을 아래에 상시 노출하는 방식으로 바꿨다.
- `ToggleGroup`의 기본 `className`에 `w-fit`이 있다. grid로 쓰려면 `w-full`을 같이 줘야 한다.
- **Radix `SelectItem`의 value에 제어 문자를 넣지 말 것.** 처음에 "없음" 항목의 value로
  `"\0clear"`(NUL 시작)를 썼더니 항목은 렌더링되는데 클릭해도 `onValueChange`가 안 불리고
  체크 상태도 안 잡혔다 — 에러 없이 조용히 무시된다. 평범한 문자열 + 접두사로 해결했다.

## 남아 있는 lint 에러

`ui/badge.tsx`, `ui/button.tsx`, `ui/toggle.tsx`의 `react-refresh/only-export-components`
3건. shadcn CLI가 생성한 파일이고 컴포넌트와 `cva` variants를 함께 export하는 구조 때문이다.
생성 파일이라 손대지 않았다.
