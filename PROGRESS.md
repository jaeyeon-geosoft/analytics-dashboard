# 진행 상황

마지막 갱신: 2026-08-03

**이 문서는 "지금 참인 것"만 담는다.** 왜 그렇게 됐는지, 뭘 틀렸었는지, 실제로 잰
숫자는 [docs/lessons.md](docs/lessons.md)에 있다. 규칙 자체는 [CLAUDE.md](CLAUDE.md)가
한 곳에서 들고 있다. 관리 방법은 맨 아래 [문서 관리](#문서-관리).

## 지금 단계

**한 파일에서 차트를 4장까지 동시에 본다.** 파일을 열면 차트가 그려지고, 거기서 카드를
늘려 서로 다른 종류·매핑으로 나란히 놓을 수 있다.

```
파일 → 확장자·크기 검증 → 인코딩 감지 → 파싱(CSV/TSV·Excel)
     → 컬럼 타입 추론 → 매핑 자동 채움 → 차트 카드 1~4장 (각각 9종 + 표 보기)
```

CSV/TSV와 Excel(`.xlsx`/`.xls`) 모두 열리고, 여러 시트와 헤더가 1행이 아닌 파일도
지정해서 읽는다. 추론이 틀리면 사이드바에서 고칠 수 있고, 고치면 **모든 카드의** 매핑
후보와 차트가 따라 바뀐다.

**다음에 할 만한 것:** 아직 아무것도 정해지지 않았다 — 정렬·필터(범주가 많을 때 골라
보기), 차트 PNG 내보내기, 사용 설명서에 스크린샷 넣기.

## 화면 구조

```
┌─ AppHeader (h-14) ──────────────────────────────────────┐
│  워드마크                 [파일 열기] │ [테마 토글]      │
├─ SettingsSidebar (w-72) ─┬─ main ───────────────────────┤
│  데이터셋                 │ ┌ DatasetBar ─────────────┐ │
│    파일 · 시트 · 헤더 행   │ │ 파일명·행수·파일 경고     │ │
│  컬럼 (타입 배지 + 수정)   │ │            [+ 차트 추가] │ │
│  ─ [n] 차트 n ─────       │ └─────────────────────────┘ │
│  차트 종류 (9종 피커)      │ ┌ ChartCard ─┐┌ ChartCard ┐│
│  매핑 (종류별) + 집계      │ │[1] 매핑 요약││[2] …      ││
│  ─ 로컬 전용 계약 3줄 ─    │ │  차트/표  × ││           ││
│                          │ └────────────┘└───────────┘│
└──────────────────────────┴─────────────────────────────┘
```

- 시트 선택은 Excel에서 시트가 2개 이상일 때만, 헤더 행은 파일을 연 뒤에만 나온다.
- 사이드바의 `[n] 차트 n` 머리줄은 **카드가 2장 이상일 때만** 나온다.
- 용어(매핑·범주·분할·집계)는 섹션마다 ⓘ(`Hint`) 하나로 설명한다 — 헤더 행, 컬럼, 매핑 세 곳.
- `lg` 미만은 캔버스가 위·설정이 아래로 쌓이고, 카드는 `xl` 이상에서만 2열.
- 캔버스는 `relative` + 자식 `absolute inset-0`. 높이를 백분율(`h-full`)로 잡으면 무너진다.

→ 근거와 확인한 화면 크기: [lessons: 화면](docs/lessons.md#화면)

| 파일 | 역할 |
|---|---|
| `src/App.tsx` | 상태 보유(`CanvasState`, `ChartSpec[]`, 선택 카드), 파일 핸들러 |
| `src/components/app-header.tsx` | 워드마크, 파일 열기, 테마 토글 |
| `src/components/settings-sidebar.tsx` | 데이터셋(시트·헤더 행) / 컬럼 / **선택된 카드**의 차트 종류·매핑·집계 / 로컬 전용 계약 |
| `src/components/chart-type-picker.tsx` | 9종 피커(막대/가로 막대/누적 막대/선/영역/산점도/궤적/분포/원형) + 직접 그린 마크 글리프 |
| `src/lib/chart-spec.ts` | `ChartSpec`(종류·매핑·집계), `createChart()`·`duplicateChart()`, 상한 `MAX_CHARTS` |
| `src/components/chart-canvas.tsx` | 캔버스 4개 상태 + `DatasetBar` + 카드 그리드(`ChartCard`) |
| `src/components/file-dropzone.tsx` | 드래그&드롭 + 파일 선택 |
| `src/components/theme-toggle.tsx` | 라이트/다크 (`localStorage`, 데이터 아님) |
| `src/lib/file-constraints.ts` | 확장자 목록, 크기 상한(50MB), `validateFile()` |
| `src/components/column-list.tsx` | 컬럼별 타입 배지 + 덮어쓰기, 불확실 경고 |
| `src/lib/parse-file.ts` | 인코딩 감지, CSV(papaparse)·Excel(SheetJS) 파싱, 헤더 행, 행 상한(10만) |
| `src/lib/infer-types.ts` | 컬럼 타입 추론 (숫자/날짜/범주 + 신뢰도 + 고유값 수) |
| `src/lib/mapping-slots.ts` | 슬롯 정의, 후보 필터, 자동 채움, 무효 선택 정리, 슬롯 잠금 |
| `src/lib/aggregate.ts` | 집계(합계/평균/개수) + 히스토그램 구간 나누기 + 차트가 먹을 모양으로 변환 |
| `src/lib/derive-column.ts` | 직전 행과의 시차(초) 컬럼 만들기. **파생 값은 여기 하나뿐** |
| `src/components/chart-view.tsx` | Recharts 9종 렌더러 (보이는 창만 그린다) |
| `src/components/data-table.tsx` | 차트와 같은 집계 결과의 표 보기 (보이는 행만 그린다) |

## 파싱

`src/App.tsx`의 `handleFile()`이 흐름을 잡는다:

```
validateFile()  확장자·크기 (파싱 없이)
  → loading
  → parseFile()  확장자로 갈라 CSV(papaparse) 또는 Excel(SheetJS)
  → shape()    행 배열에서 헤더 행을 뽑아 객체 행으로
  → 컬럼 0개 / 행 0개면 error
  → ready { dataset, data }
```

`ParsedFile`은 `columns` / `rows` / `encoding` / `truncated` / `errorCount` / `sheets` /
`sheet` / `headerRow` / `preview`(헤더 행 후보용 앞 10행)를 담는다.

CSV든 Excel이든 **행 배열(`string[][]`)로 읽은 뒤 `shape()`가 헤더를 뽑는다** — 제목 줄이
위에 붙은 리포트성 파일을 지원하려면 이 단계가 공통이어야 한다. 앞 64KB로 인코딩을
감지하고(UTF-8 실패 시 EUC-KR), 구분자도 앞 64KB 샘플로 먼저 정한 뒤 넘긴다.

→ 청크 스트리밍을 안 쓰는 이유, Excel 옵션, 빈 줄과 구분자 추측: [lessons: 파싱](docs/lessons.md#파싱)

## 컬럼 타입 추론

전체에 걸쳐 띄엄띄엄 200개를 뽑아 **숫자/날짜/범주 세 종류로만** 분류한다. 빈 값(`""`,
`-`, `NA`, `null` …)은 세지 않는다. 샘플의 80% 이상이 맞아야 숫자·날짜라고 주장하고,
아니면 범주다.

`ColumnInfo`는 `type`(현재) / `inferred`(추론 원본) / `confidence` / `sampled` /
`distinctCount`를 들고 있다. `type !== inferred`면 사용자가 손댄 것이고, 그때는 경고를 더
띄우지 않는다. 고유값은 50까지만 세고 멈춘다(`distinctCapped`).

→ 날짜를 먼저 보는 이유, 선행 0·분기 같은 경계 사례: [lessons: 타입 추론](docs/lessons.md#타입-추론)

### 파생 컬럼 — 시차 하나뿐

시각이 든 날짜 컬럼 옆에 ⏱ 버튼이 붙고, 누르면 `"<컬럼> 시차(초)"` 숫자 컬럼이 생긴다
(`src/lib/derive-column.ts`). 기준은 **파일에 적힌 직전 행**이다 — 로그는 이미 시간 순으로
쌓여 있어서 여기서 다시 정렬하면 원본에 없던 순서를 지어내게 된다. 첫 행은 빈 칸.

장비 로그의 "규칙적으로 쏘고 있나"는 값이 아니라 행과 행 **사이**에 있는데 그 수가 파일
어디에도 없어서 만든다. **여기서 멈춘다** — 임의 수식을 계산하기 시작하면 이 도구는
시각화 도구가 아니라 ETL이 된다.

파싱은 ISO 8601 순간만 받는다(`2024-10-16T04:00:09.926643Z`). 날짜만 있고 시각이 없는
형식은 일부러 뺐다 — 초 시차가 0 아니면 86400만 나와서 아무것도 못 읽는다. `new Date()`에
아무 문자열이나 넘기지 않고 패턴을 먼저 맞춘 뒤 `Date.parse`에 넘긴다.

## 차트 렌더링

집계는 `aggregate.ts`가 맡는다. 범주(또는 X축)로 묶고 합계/평균/개수 중 하나로 줄인다.
막대·원형은 큰 것부터, 선·영역은 X축 순서대로 정렬한다. 산점도·궤적만 집계하지 않는다
(행 하나가 점 하나).

**분포(히스토그램)는 정렬하지 않는다.** 구간은 순서 자체가 의미라서 큰 것부터 세우면
분포가 아니게 된다. 구간 폭은 Freedman–Diaconis(`2·IQR/∛n`)로 자동이고 1·2·5 사다리에
맞춰 떨어뜨린다 — 사분위를 쓰니 꼬리의 극단값에 폭이 끌려가지 않는다. 폭은 축 이름에
적는다(`(구간 0.5)`). 값이 하나뿐인 컬럼은 없는 범위를 지어내지 않고 막대 하나로 끝낸다.
막대와 같은 `ChartFrame`을 내놓아 **렌더러도 표 보기도 그대로 쓴다.**

**궤적은 산점도와 같은 프레임을 쓰고 점을 잇기만 한다**(`buildScatterFrame`). 잇는 순서는
정렬한 순서가 아니라 **파일에 적힌 행 순서**다 — 되돌아오는 경로를 X로 정렬하면 모양이
뭉개지기 때문이다. 그래서 파일이 그리려는 순서대로 정렬돼 있어야 한다.

지금 참인 것:

- **막대·표는 보이는 창만 그린다.** 차트는 마크 하나에 28px(`usePlotWindow`), 표는
  행 높이 28px 고정(`h-7`). 나머지는 스크롤바로 옮긴다. 범주가 몇 개든 비용이 같다.
- **선·영역은 창을 쓰지 않는다.** 시계열은 개별 점이 아니라 모양이 정보라, 자르면 5시간
  파형을 못 본다. 대신 3,000점으로 **줄이되 구간마다 최소·최대를 남겨**(`downsample`)
  스파이크를 지키고, 줄였다는 사실은 카드에 알린다. 통계는 줄이기 전 전체에서 낸다.
  100,000행 기준 39초 → 0.8초.
- **값 축은 전체 데이터 기준으로 고정**한다. 창을 옮겨도 축이 흔들리지 않는다.
- **산점도·궤적의 축만 0에서 시작하지 않는다**(`domain={["auto","auto"]}`). 여기서는 길이가
  아니라 위치가 값이라, 0을 끼우면 위·경도처럼 원점에서 먼 데이터가 구석에 뭉친다.
  "축은 0에서 시작"은 막대 차트 규칙이다.
- **눈금 라벨은 공통 앞부분을 떼고**(`sharedPrefix`) 뗀 부분은 축 이름에 한 번 적는다.
  툴팁·표는 원본 그대로.
- **기준선은 카드 색 4px을 깔고 그 위에 텍스트 색 파선**을 얹는다. 격자선은 뒤로 물러나야
  하지만 기준선은 읽으라고 있는 주석이라 다르다 — 회색으로 두면 채도 높은 막대 위에서
  묻힌다. 값은 **선이 아니라 축 이름에** 적는다(`· ┄ 중앙값 1.08`). 선 위에 붙이면 최대값
  직접 라벨과 자리를 다투는데, 분포에서는 기준선이 든 구간이 곧 최빈 구간이라 거의 항상
  겹친다. 히스토그램만 x축이 범주축이라 값이 든 **구간에 스냅**된다(정확한 수는 축 이름이 든다).
- **상한:** 원형 조각 6개(나머지는 "기타"), 산점도 점 3,000개. 둘 다 화면에 알린다.
- **"그리는 중" 표시는 산점도에만.** 무거운 계산과 `표 → 차트` 전환 두 곳이고, 띄울지는
  시작 전에 정한다(`looksSlow`). 나머지는 창으로 줄어 띄울 만큼 느리지 않다.
- 스크롤바는 절대 플롯 안에 두지 않는다(무한 재측정).

→ 측정치 표, Recharts 함정 표, 실패한 접근들: [lessons: 차트 렌더링](docs/lessons.md#차트-렌더링)

### 카드 여러 장

`ChartSpec`(종류·매핑·집계) 하나가 카드 한 장이고 App이 배열로 든다. 데이터·컬럼은 파일
단위라 공유한다. **상한 4장.** 계산은 카드 순서대로 한 장씩 미룬다(rAF `2 + 순번`번).
스크롤은 그리드가 맡고 카드 안쪽은 스크롤하지 않는다. 파일 단위 경고는 `DatasetBar`,
차트 단위 경고는 카드 헤더.

→ 상한이 4인 이유, 행 높이·열 수 결정: [lessons: 카드 여러 장](docs/lessons.md#카드-여러-장)

## 차트 종류별 매핑 슬롯

사이드바의 "매핑" 섹션은 선택된 차트 종류에 따라 라벨과 슬롯 개수가 바뀐다.
정의는 `src/lib/mapping-slots.ts`의 `MAPPING_SLOTS`에 있다.

| 종류 | 슬롯 |
|---|---|
| 막대 / 가로 막대 | 범주, 값, 분할*(선택)* |
| 누적 막대 | 범주, 값, 누적 기준 |
| 선 | X축, Y축(좌), Y축(우)*(선택)*, 분할*(선택)* |
| 영역 / 산점도 / 궤적 | X축, Y축, 분할*(선택)* |
| 분포 | 값 *(하나뿐)* |

**기준선**은 분포·선·영역에만 있다(`allowsReference`). 평균/중앙값 둘 중 하나고 임의의 숫자는
받지 않는다 — 자유 입력을 열면 그게 무슨 값인지 아무도 모르는 선이 차트에 남는다. 값 축이
하나로 정해지는 곳만 주고, 이중 축이 켜진 선 차트는 계산 쪽에서 다시 걸러낸다.
| 원형 | 범주, 값 |

| 슬롯 | 받는 타입 |
|---|---|
| 값 / Y축 | 숫자 |
| 범주 | 범주, 날짜 |
| X축 (선·영역) | 숫자, 날짜, 범주 — 순서만 있으면 되므로 분기 같은 범주도 정당하다 |
| X축·Y축 (산점도·궤적) | 숫자 (관계를 보려면 양쪽이 수치여야 한다) |
| 분할 / 누적 기준 | 범주, 날짜 **+ 고유값 8개 이하** — 단 산점도·궤적은 **3개 이하** |

- 선택 슬롯은 placeholder가 `없음`, 필수 슬롯은 `컬럼 선택`. 후보에 없는 컬럼은 아예 안 나오고,
  후보가 하나도 없으면 왜 없는지를 placeholder에 적는다.
- 막대·원형은 역할 이름(범주/값)을, 선·산점도는 축 이름(X축/Y축)을 쓴다 — 후자는 양쪽이
  모두 수치라서 역할로 부를 이름이 없다.
- 차트 종류를 바꿔도 **같은 key면 고른 컬럼이 살아남는다.** 지금 종류에 없는 슬롯의 값도
  버리지 않는다.
- `Y축(우)`는 선 차트에만 있고 **분할·개수 집계와 함께 못 쓴다.** 한쪽이 켜지면 다른 쪽을
  잠그고 왜 잠겼는지 트리거에 적는다(`lockedReason`).

→ 오른쪽 축을 허용한 조건, 자동 채움 순서, `none` 접두사: [lessons: 매핑](docs/lessons.md#매핑)

## 미결정 사항

- **큰 Excel의 시트 전환** — 지금은 원본을 다시 읽는다. 느려지면 워크북을 들고 있는 쪽으로.
- **상태관리** — 카드 배열까지도 아직 `useState`로 충분. prop이 더 길어지면 재검토

## 남아 있는 lint 에러

`ui/badge.tsx`, `ui/button.tsx`, `ui/toggle.tsx`의 `react-refresh/only-export-components`
3건. shadcn CLI가 생성한 파일이고 컴포넌트와 `cva` variants를 함께 export하는 구조 때문이다.
생성 파일이라 손대지 않았다.

## 문서 관리

| 문서 | 담는 것 | 언제 읽나 |
|---|---|---|
| [CLAUDE.md](CLAUDE.md) | 지켜야 할 규칙 | 항상 (자동으로 실린다) |
| PROGRESS.md (여기) | 지금 참인 상태·구조·스펙 | 세션 시작할 때 |
| [docs/lessons.md](docs/lessons.md) | 근거·측정치·틀렸던 접근·함정 | 규칙을 바꾸려 할 때, "왜 이렇게 돼 있지?"일 때 |

**여기는 append-only가 아니다.** 새 발견이 이전 결론을 뒤집으면 **이전 문장을 지운다.**
틀린 근거가 남아 있으면 다음 세션이 그걸 믿고 코드를 쓴다 — 문서가 긴 것보다 이쪽이
위험하다. 겪은 것을 남기고 싶으면 `lessons.md`로 보내고 여기엔 한 줄 요약 + 링크만 둔다.
