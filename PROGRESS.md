# 진행 상황

마지막 갱신: 2026-08-04

**이 문서는 "지금 참인 것"만 담는다.** 왜 그렇게 됐는지, 뭘 틀렸었는지, 실제로 잰
숫자는 [docs/lessons.md](docs/lessons.md)에 있다. 규칙 자체는 [CLAUDE.md](CLAUDE.md)가
한 곳에서 들고 있다. 관리 방법은 맨 아래 [문서 관리](#문서-관리).

## 지금 단계

**어드민에서 만든 대시보드를 뷰어가 그대로 그린다.** 화면이 둘로 갈렸고, 둘 사이는
`Dashboard` JSON으로 오간다.

```
[어드민 :5173]  파일 → 검증 → 인코딩 감지 → 파싱 → 타입 추론 → 매핑 자동 채움
                → 카드 1~4장 (8종 + 표 보기) → 끌어 배치·크기 조절
                → [내보내기] → dashboard.json
                                     │
[뷰어  :5174]   ← [대시보드 열기] ────┘ → 검증 → 저장된 배치대로 여러 차트 렌더
```

어드민은 CSV/TSV와 Excel(`.xlsx`/`.xls`)을 열고, 여러 시트와 헤더가 1행이 아닌 파일도
지정해서 읽는다. 추론이 틀리면 사이드바에서 고칠 수 있고, 고치면 **모든 카드의** 매핑
후보와 차트가 따라 바뀐다. 뷰어는 설정 UI 없이 받아서 그리기만 한다.

**JSON은 API가 붙기 전까지의 임시 통로다.** 모양은 이미 최종형이라(→ [계약](#대시보드-계약))
나중에 어드민의 `내보내기`가 `POST`로, 뷰어의 `loadDashboardFile()`이 `fetch`로
바뀌기만 하면 된다.

**다음에 할 것 — 어드민 다중 파일.** 계약(`Dashboard.datasets`)은 이미 파일 여러 개를
받게 돼 있고 뷰어도 그렇게 그리는데, **어드민이 아직 파일 하나만 다룬다**(`App.tsx`가
`CanvasState` 하나를 들고 카드 전부가 그 데이터를 공유). 차트마다 다른 파일을 붙이려면
상태 모델을 데이터셋 배열로 갈아야 한다. 지금까지 중 제일 큰 수술이고, **뷰어는 안
건드려도 된다.**

**그 외 — 실제 데이터를 대보다 막힌 것들이라 근거가 있다:**

| 필요한 것 | 왜 |
|---|---|
| **숫자를 구간으로 나누기** | 분포를 보려면 있어야 한다. 히스토그램 종류로 한 번 만들었다가 뺐다 — [아래](#분포-차트를-넣었다-뺀-이유) |
| **값 필터** (행 골라 보기) | 고유값이 슬롯 상한을 넘으면 그 컬럼은 후보에서 빠진다. 항로 15개짜리 파일에서 3개만 보려는 게 안 됐다 |
| **값 컬럼 3개 이상** | 지금 최대 2개(`Y축(좌)`/`Y축(우)`)고, 그것도 스케일이 다를 때 쓰라고 만든 슬롯이다. MIN/MID/MAX 같은 동일 스케일 3개는 못 그린다 |
| 차트 제목 입력란 | 지금은 매핑 요약(`지역 → 매출`)을 제목으로 쓴다. 계약에는 `title` 자리가 이미 있다 |
| 산점도·궤적 점 상한(3,000) | 원시 GPS 항적은 수만 점이라 뒷부분이 잘린다 |
| 사분위 통계 | 박스플롯의 전제. 지금 집계는 합계/평균/개수뿐 |

그 외: 차트 PNG 내보내기, 사용 설명서에 스크린샷 넣기.

### 목표로 삼은 차트 9장

분석가가 엑셀로 그려둔 화면 9장을 그대로 낼 수 있는지가 기준이다. 샘플 로그는
`Downloads/SampleData/`에 있다.

| 목표 | 필요한 것 | 상태 | 확인용 파일 |
|---|---|---|---|
| 주요 항로 궤적 (경도×위도, 항로 3개) | 궤적 | ✅ **화면으로 확인** | `ECDIS_WAYPOINTS.csv` — 단, 항로 3개 × 발행 1회로 추려야 한다 |
| VLW 수신 주기 추이 (+ 평균선) | 선 · 시차 · 기준선 | ✅ **화면으로 확인** | `SPEEDLOG_VLW.csv` + ⏱ 시차 |
| 목표 웨이포인트 번호 추이 | 선 | ❓ 미검증 — 될 것으로 보임 | 미확보 (`ROUTE_ETAWPNO` 필요) |
| 누적 항해거리 2선 | 선 · 이중 축 | ❓ 미검증 | `SPEEDLOG_VLW.csv` (`TOTALCUMULATIVE…` + `…SINCERESET`) |
| 속도 Knots vs km/h | 선 · 이중 축 | ❓ 미검증 | 미확보 (`SPEEDN` 필요) |
| 항적 + Fix Quality 색 | 궤적 · 색 2분할 | ⚠️ 그려지되 **3,000점 상한에 뒷부분이 잘림** | 미확보 |
| CAN 수신 주기 분포 (+ 명목 주기선) | **구간 나누기** | ❌ 지금 불가 | `CANTHROTTLE_RUDDERSCALE.csv` + ⏱ 시차 |
| 스케일 MIN/MID/MAX 3선 | **값 컬럼 3개** | ❌ 지금 불가 (최대 2개) | `CANTHROTTLE_RUDDERSCALE.csv` |
| DOP 박스플롯 | **박스플롯 종류 + 사분위** | ❌ 지금 불가 — 유일하게 Recharts에 없어서 커스텀 shape이 필요하다 | 미확보 |

이중 축 두 건은 슬롯이 있으니 될 것으로 보지만 **실제로 안 해봤다.** 되는지부터 확인할 것.

### 분포 차트를 넣었다 뺀 이유

숫자 컬럼을 구간으로 묶어 세는 종류를 만들었다가(Freedman–Diaconis 자동 폭) 지웠다.
정렬이 축 타입을 보게 고쳐지고 막대에 기준선이 붙으면서, **구간·건수 컬럼이 이미 있는
파일이면 막대로 같은 그림이 나오기** 때문이다.

**대가:** 그 두 컬럼이 없는 원본 로그(예: 타임스탬프만 있는 CAN 로그)에서는 분포를 볼
방법이 지금 없다. 다시 필요해지면 `git show 1ee151a`에 구현이 통째로 있다.

## 화면 구조

**어드민** (`src/admin/index.html` → `:5173`)

```
┌─ AppHeader (h-14) ──────────────────────────────────────┐
│  차트 설정               [파일 열기] │ [테마 토글]       │
├─ SettingsSidebar (w-72) ─┬─ main ───────────────────────┤
│  데이터셋                 │ ┌ DatasetBar ─────────────┐ │
│    파일 · 시트 · 헤더 행   │ │ 파일명·행수·파일 경고     │ │
│  컬럼 (타입 배지 + ⏱시차)  │ │  [+ 차트 추가] [내보내기]│ │
│  ─ [n] 차트 n ─────       │ └─────────────────────────┘ │
│  차트 종류 (8종 피커)      │ ┌ GridLayout ─────────────┐ │
│  매핑 + 집계 + 기준선      │ │ [1] 카드  ⌐││ [2] 카드 ⌐│ │
│  ─ 데이터 취급 3줄 ─       │ │  ↑ 끌어 옮기고 늘린다     │ │
│                          │ └─────────────────────────┘ │
└──────────────────────────┴─────────────────────────────┘
```

**뷰어** (`src/viewer/index.html` → `:5174`) — 설정 UI 없음

```
┌─ header (h-14) ─────────────────────────────────────────┐
│  대시보드 제목 / 차트 n장 · 파일명들   [열기] │ [테마]   │
├─────────────────────────────────────────────────────────┤
│  CSS Grid — 저장된 x/y/w/h 그대로 (스크롤은 여기가 맡음) │
└─────────────────────────────────────────────────────────┘
```

- 시트 선택은 Excel에서 시트가 2개 이상일 때만, 헤더 행은 파일을 연 뒤에만 나온다.
- 사이드바의 `[n] 차트 n` 머리줄은 **카드가 2장 이상일 때만** 나온다.
- 용어(매핑·범주·분할·집계)는 섹션마다 ⓘ(`Hint`) 하나로 설명한다 — 헤더 행, 컬럼, 매핑 세 곳.
- `lg` 미만은 캔버스가 위·설정이 아래로 쌓인다.
- 캔버스는 `relative` + 자식 `absolute inset-0`. 높이를 백분율(`h-full`)로 잡으면 무너진다.
- **카드는 `overflow-hidden`이다.** 크기를 줄일 수 있게 되면서 안 가두면 차트가
  테두리 밖으로 흘러나온다.

→ 근거와 확인한 화면 크기: [lessons: 화면](docs/lessons.md#화면)

### 배치

**어드민은 `react-grid-layout`, 뷰어는 CSS Grid.** 뷰어는 끌지도 늘리지도 않으므로
저장된 `x/y/w/h`를 그대로 놓으면 되고, **같은 상수를 쓰기 때문에 기하가 일치한다** —
`GRID_COLS`(12) · `GRID_ROW_HEIGHT`(40px) · `GRID_MARGIN`(12px)는 `shared/lib/dashboard.ts`에
있다. 한쪽만 바꾸면 같은 대시보드가 두 화면에서 다르게 배치된다.

- 기본 칸: `w=6, h=8`(404px), 한 줄에 두 장.
- 하한 `minW=3` · `minH=7`. 세로 하한은 눈대중이 아니라 **내용 높이에서 나온 수**다 —
  머리줄 ~55px + 플롯 `min-h-64`(256px) = 351px, 7칸 = 352px.
- 세로 압축이 기본이라 카드를 아래로 끌어도 빈칸이 있으면 위로 당겨 올라온다.
  열 사이 이동과 크기 조절은 그대로 된다.
- rgl 기본 CSS는 `src/admin/grid.css`가 덮어쓴다(빨간 자리표시, 겹쳐 그려지는 삼각형).

→ [lessons: 배치](docs/lessons.md#배치)

**도메인 타입은 `lib/`에 둔다.** `ChartType`·`CanvasState`가 컴포넌트 파일에 있으면
`lib/aggregate.ts`가 `components/`를 import하게 된다 — 방향이 거꾸로다.

**`src/`는 `shared/` · `admin/` · `viewer/` 셋으로 갈려 있다**(규칙은 [CLAUDE.md](CLAUDE.md#레포-구조),
eslint가 강제). **앱 둘은 dev 서버도 빌드도 따로다** — Vite `root`가 앱 폴더라
`npm run dev`(:5173)는 어드민만, `npm run dev:viewer`(:5174)는 뷰어만 연다.
산출물은 `dist-admin/index.html` · `dist-viewer/index.html`.

| 파일 | 역할 |
|---|---|
| `src/admin/App.tsx` | 상태 보유(`CanvasState`, `ChartSpec[]`, **배치 `Layout`**, 선택 카드), 파일·내보내기 핸들러 |
| `src/admin/components/app-header.tsx` | 워드마크, 파일 열기, 테마 토글 |
| `src/admin/components/settings-sidebar.tsx` | 데이터셋(시트·헤더 행) / 컬럼 / **선택된 카드**의 차트 종류·매핑·집계·기준선 / 데이터 취급 안내 |
| `src/admin/components/chart-type-picker.tsx` | 8종 피커(막대/가로 막대/누적 막대/선/영역/산점도/궤적/원형) + 직접 그린 마크 글리프 |
| `src/shared/lib/chart-types.ts` | `ChartType` 8종. 도메인 타입이라 피커가 아니라 여기 있다 |
| `src/shared/lib/dataset.ts` | `DataFrame`(`columns`+`rows`) — 렌더러가 먹는 최소 형태. 어드민의 `ParsedFile`이 그대로 대입되고, 뷰어는 API 응답에서 이 모양만 만들면 된다 |
| `src/admin/lib/canvas-state.ts` | `CanvasState`(empty/loading/error/ready), `Dataset` |
| `src/shared/lib/chart-spec.ts` | `ChartSpec`(종류·매핑·집계·기준선), `createChart()`·`duplicateChart()`, **명세를 고치는 규칙**(`withChartType`·`withColumns`·`withMapping`), 상한 `MAX_CHARTS` |
| `src/admin/components/chart-canvas.tsx` | 캔버스 4개 상태 + `DatasetBar` + `ChartGrid`(rgl) |
| `src/shared/components/chart-card.tsx` | 카드 한 장 — 계산 지연(`useDeferredPlot`), 차트↔표 토글, 카드 단위 경고 |
| `src/admin/components/file-dropzone.tsx` | 드래그&드롭 + 파일 선택 |
| `src/shared/components/theme-toggle.tsx` | 라이트/다크 (`localStorage`, 데이터 아님) |
| `src/admin/lib/file-constraints.ts` | 확장자 목록, 크기 상한(50MB), `validateFile()` |
| `src/admin/components/column-list.tsx` | 컬럼별 타입 배지 + 덮어쓰기, 불확실 경고, 시차 컬럼 만들기(⏱) |
| `src/admin/lib/parse-file.ts` | 인코딩 감지, CSV(papaparse)·Excel(SheetJS) 파싱, 헤더 행, 행 상한(10만) |
| `src/shared/lib/infer-types.ts` | 컬럼 타입 추론 (숫자/날짜/범주 + 신뢰도 + 고유값 수) |
| `src/shared/lib/mapping-slots.ts` | 슬롯 정의, 후보 필터, 자동 채움, 무효 선택 정리, 슬롯 잠금 |
| `src/shared/lib/aggregate.ts` | 집계(합계/평균/개수) + 차트가 먹을 모양으로 변환. 진입점은 `buildPlot()` 하나 |
| `src/admin/lib/derive-column.ts` | 직전 행과의 시차(초) 컬럼 만들기. **파생 값은 여기 하나뿐** |
| `src/shared/components/chart-view.tsx` | Recharts 렌더러 — `TimelineView`(선·영역) / `BarView`(막대 3종) / `PieView` / `ScatterView` |
| `src/shared/components/data-table.tsx` | 차트와 같은 집계 결과의 표 보기 (보이는 행만 그린다) |
| `src/shared/lib/dashboard.ts` | **계약.** `Dashboard` 타입, `parseDashboard()` 검증기, 그리드 규격 상수 |
| `src/admin/lib/export-dashboard.ts` | 지금 어드민 상태 → `Dashboard` → JSON 다운로드. **나중에 여기가 POST** |
| `src/admin/lib/chart-layout.ts` | 새 카드의 기본 칸, 하한(`minW`/`minH`), 카드↔배치 맞추기(`syncLayout`) |
| `src/admin/grid.css` | rgl 기본 CSS 덮어쓰기 (자리표시·크기 손잡이) |
| `src/viewer/viewer-app.tsx` | 뷰어 화면 — 빈 상태 / 실패 이유 / 대시보드 |
| `src/viewer/load-dashboard.ts` | 대시보드를 가져오는 **유일한 자리**. **나중에 여기가 fetch** |
| `src/viewer/components/dashboard-grid.tsx` | 저장된 배치를 CSS Grid로 그린다 |

## 대시보드 계약

어드민이 만들고 뷰어가 그리는 것. 정의와 검증기는 `src/shared/lib/dashboard.ts`.
**지금은 JSON 파일로 오가지만 API가 붙으면 그대로 응답 본문이 된다** — 백엔드에 넘길
스펙 문서이기도 하다.

```ts
Dashboard {
  format: 1                 // 모르는 번호면 뷰어가 그리지 않고 그 사실을 말한다
  id, title
  datasets: [{ id, name, columns: ColumnInfo[], data: DataFrame }]
  charts:   [{ id, title, datasetId, spec: ChartSpec, layout: {x,y,w,h} }]
}
```

- **`datasets`를 차트와 분리한다.** 차트마다 데이터를 안고 있으면 같은 파일을 보는
  차트 3장에 10만 행이 3벌 들어간다. 차트는 `datasetId`로 가리키기만 한다.
- **`columns`(타입 추론 결과)를 반드시 함께 담는다.** 정렬 규칙과 매핑 후보가 컬럼
  타입을 보고 갈리는데 사용자가 어드민에서 추론을 고쳤을 수 있다. 뷰어에서 다시
  추론하면 어드민에서 본 것과 다른 차트가 나온다(절대 원칙 1).
- `parseDashboard()`는 **구조와 참조 무결성만** 본다. 빈 매핑이나 없는 컬럼 이름은
  렌더러가 이미 처리하고, 여기서 또 막으면 규칙이 두 곳이 된다.
- 아는 차트 종류는 배열을 따로 두지 않고 `MAPPING_SLOTS`의 키를 쓴다. `Record<ChartType, …>`라
  종류가 늘면 타입 검사가 채우기를 강제해서 어긋날 수 없다.

## 파싱

`src/admin/App.tsx`의 `handleFile()`이 흐름을 잡는다:

```
validateFile()  확장자·크기 (파싱 없이)
  → loading
  → parseFile()  확장자로 갈라 CSV(papaparse) 또는 Excel(SheetJS)
  → shape()    행 배열에서 헤더 행을 뽑아 객체 행으로
  → 컬럼 0개 / 행 0개면 error
  → ready { dataset, data }
```

`ParsedFile`은 `columns` / `rows` / `encoding` / `truncated` / `errorCount` / `sheets` /
`sheet` / `headerRow` / `preview`(헤더 행 후보용 앞 10행)를 담는다.

CSV든 Excel이든 **행 배열(`string[][]`)로 읽은 뒤 `shape()`가 헤더를 뽑는다** — 제목 줄이
위에 붙은 리포트성 파일을 지원하려면 이 단계가 공통이어야 한다. 앞 64KB로 인코딩을
감지하고(UTF-8 실패 시 EUC-KR), 구분자도 앞 64KB 샘플로 먼저 정한 뒤 넘긴다.

→ 청크 스트리밍을 안 쓰는 이유, Excel 옵션, 빈 줄과 구분자 추측: [lessons: 파싱](docs/lessons.md#파싱)

## 컬럼 타입 추론

전체에 걸쳐 띄엄띄엄 200개를 뽑아 **숫자/날짜/범주 세 종류로만** 분류한다. 빈 값(`""`,
`-`, `NA`, `null` …)은 세지 않는다. 샘플의 80% 이상이 맞아야 숫자·날짜라고 주장하고,
아니면 범주다.

`ColumnInfo`는 `type`(현재) / `inferred`(추론 원본) / `confidence` / `sampled` /
`distinctCount`를 들고 있다. `type !== inferred`면 사용자가 손댄 것이고, 그때는 경고를 더
띄우지 않는다. 고유값은 50까지만 세고 멈춘다(`distinctCapped`).

→ 날짜를 먼저 보는 이유, 선행 0·분기 같은 경계 사례: [lessons: 타입 추론](docs/lessons.md#타입-추론)

### 파생 컬럼 — 시차 하나뿐

시각이 든 날짜 컬럼 옆에 ⏱ 버튼이 붙고, 누르면 `"<컬럼> 시차(초)"` 숫자 컬럼이 생긴다
(`src/admin/lib/derive-column.ts`). 기준은 **파일에 적힌 직전 행**이다 — 로그는 이미 시간 순으로
쌓여 있어서 여기서 다시 정렬하면 원본에 없던 순서를 지어내게 된다. 첫 행은 빈 칸.

장비 로그의 "규칙적으로 쏘고 있나"는 값이 아니라 행과 행 **사이**에 있는데 그 수가 파일
어디에도 없어서 만든다. **여기서 멈춘다** — 임의 수식을 계산하기 시작하면 이 도구는
시각화 도구가 아니라 ETL이 된다.

파싱은 ISO 8601 순간만 받는다(`2024-10-16T04:00:09.926643Z`). 날짜만 있고 시각이 없는
형식은 일부러 뺐다 — 초 시차가 0 아니면 86400만 나와서 아무것도 못 읽는다. `new Date()`에
아무 문자열이나 넘기지 않고 패턴을 먼저 맞춘 뒤 `Date.parse`에 넘긴다.

## 차트 렌더링

집계는 `aggregate.ts`가 맡고, **진입점은 `buildPlot()` 하나다.** 차트 종류를 보고 어느
프레임을 만들지 정하는 곳이 여기뿐이라, 결과는 `PlotData`(`kind: "cartesian" | "scatter"`)로
갈라져 나온다 — 읽는 쪽이 종류를 다시 물어 어느 프레임이 찼는지 맞출 필요가 없다.

범주(또는 X축)로 묶고 합계/평균/개수 중 하나로 줄인다.
정렬은 **축이 순서를 가졌느냐**로 갈린다 — 날짜·숫자 축은 그 순서대로, 순서 없는
범주(제품명·지역)만 값 큰 순으로. 날짜를 값 큰 순으로 세우면 축이 거짓말을 한다(시계열
막대가 시간 역순으로 나왔다). 선·영역은 언제나 X축 순서다. 산점도·궤적만 집계하지 않는다
(행 하나가 점 하나).

**궤적은 산점도와 같은 프레임을 쓰고 점을 잇기만 한다**(`buildScatterFrame`). 잇는 순서는
정렬한 순서가 아니라 **파일에 적힌 행 순서**다 — 되돌아오는 경로를 X로 정렬하면 모양이
뭉개지기 때문이다. 그래서 파일이 그리려는 순서대로 정렬돼 있어야 한다.

지금 참인 것:

- **막대·표는 보이는 창만 그린다.** 차트는 마크 하나에 28px(`usePlotWindow`), 표는
  행 높이 28px 고정(`h-7`). 나머지는 스크롤바로 옮긴다. 범주가 몇 개든 비용이 같다.
- **선·영역은 창을 쓰지 않는다.** 시계열은 개별 점이 아니라 모양이 정보라, 자르면 5시간
  파형을 못 본다. 대신 3,000점으로 **줄이되 구간마다 최소·최대를 남겨**(`downsample`)
  스파이크를 지키고, 줄였다는 사실은 카드에 알린다. 통계는 줄이기 전 전체에서 낸다.
  100,000행 기준 39초 → 0.8초.
- **값 축은 전체 데이터 기준으로 고정**한다. 창을 옮겨도 축이 흔들리지 않는다.
- **산점도·궤적의 축만 0에서 시작하지 않는다**(`domain={["auto","auto"]}`). 여기서는 길이가
  아니라 위치가 값이라, 0을 끼우면 위·경도처럼 원점에서 먼 데이터가 구석에 뭉친다.
  "축은 0에서 시작"은 막대 차트 규칙이다.
- **눈금 라벨은 공통 앞부분을 떼고**(`sharedPrefix`) 뗀 부분은 축 이름에 한 번 적는다.
  툴팁·표는 원본 그대로.
- **시계열 X축 눈금 수는 폭에서 정한다**(`useTickInterval`). 개수를 고정하면 좁은
  카드에서 라벨이 서로 겹쳐 축이 통째로 뭉갠다. 상한은 12로 두어 **줄이기만 하고
  늘리지는 않는다.** `interval`은 그래도 숫자로 넘긴다 — Recharts에 맡기면 3,000개
  라벨의 폭을 재느라 95초가 걸린다.
- **기준선은 카드 색 4px을 깔고 그 위에 텍스트 색 파선**을 얹는다. 격자선은 뒤로 물러나야
  하지만 기준선은 읽으라고 있는 주석이라 다르다 — 회색으로 두면 채도 높은 막대 위에서
  묻힌다. 값은 **선이 아니라 축 이름에** 적는다(`· ┄ 평균 4.53`) — 선 위에 붙이면 최대값
  직접 라벨과 자리를 다툰다. 가로 막대는 축이 뒤집혀 있어 기준선도 이름도 반대쪽이다.
- **상한:** 원형 조각 6개(나머지는 "기타"), 산점도 점 3,000개. 둘 다 화면에 알린다.
- **"그리는 중" 표시는 산점도에만.** 무거운 계산과 `표 → 차트` 전환 두 곳이고, 띄울지는
  시작 전에 정한다(`looksSlow`). 나머지는 창으로 줄어 띄울 만큼 느리지 않다.
- 스크롤바는 절대 플롯 안에 두지 않는다(무한 재측정).

→ 측정치 표, Recharts 함정 표, 실패한 접근들: [lessons: 차트 렌더링](docs/lessons.md#차트-렌더링)

### 카드 여러 장

`ChartSpec`(종류·매핑·집계) 하나가 카드 한 장이고 App이 배열로 든다. 데이터·컬럼은 파일
단위라 공유한다. **상한 4장.** 계산은 카드 순서대로 한 장씩 미룬다(rAF `2 + 순번`번).
스크롤은 그리드가 맡고 카드 안쪽은 스크롤하지 않는다. 파일 단위 경고는 `DatasetBar`,
차트 단위 경고는 카드 헤더.

→ 상한이 4인 이유, 행 높이·열 수 결정: [lessons: 카드 여러 장](docs/lessons.md#카드-여러-장)

## 차트 종류별 매핑 슬롯

사이드바의 "매핑" 섹션은 선택된 차트 종류에 따라 라벨과 슬롯 개수가 바뀐다.
정의는 `src/shared/lib/mapping-slots.ts`의 `MAPPING_SLOTS`에 있다.

| 종류 | 슬롯 |
|---|---|
| 막대 / 가로 막대 | 범주, 값, 분할*(선택)* |
| 누적 막대 | 범주, 값, 누적 기준 |
| 선 | X축, Y축(좌), Y축(우)*(선택)*, 분할*(선택)* |
| 영역 / 산점도 / 궤적 | X축, Y축, 분할*(선택)* |
| 원형 | 범주, 값 |

**기준선**은 막대·가로 막대·선·영역에 있다(`allowsReference`). 평균/중앙값 둘 중 하나고
임의의 숫자는 받지 않는다 — 자유 입력을 열면 그게 무슨 값인지 아무도 모르는 선이 차트에
남는다. 값 축이 하나로 정해지는 곳만 주고, 이중 축이 켜진 선 차트는 계산 쪽에서 걸러낸다.

| 슬롯 | 받는 타입 |
|---|---|
| 값 / Y축 | 숫자 |
| 범주 | 범주, 날짜 |
| X축 (선·영역) | 숫자, 날짜, 범주 — 순서만 있으면 되므로 분기 같은 범주도 정당하다 |
| X축·Y축 (산점도·궤적) | 숫자 (관계를 보려면 양쪽이 수치여야 한다) |
| 분할 / 누적 기준 | 범주, 날짜 **+ 고유값 8개 이하** — 단 산점도·궤적은 **3개 이하** |

- 선택 슬롯은 placeholder가 `없음`, 필수 슬롯은 `컬럼 선택`. 후보에 없는 컬럼은 아예 안 나오고,
  후보가 하나도 없으면 왜 없는지를 placeholder에 적는다.
- 막대·원형은 역할 이름(범주/값)을, 선·산점도는 축 이름(X축/Y축)을 쓴다 — 후자는 양쪽이
  모두 수치라서 역할로 부를 이름이 없다.
- 차트 종류를 바꿔도 **같은 key면 고른 컬럼이 살아남는다.** 지금 종류에 없는 슬롯의 값도
  버리지 않는다.
- `Y축(우)`는 선 차트에만 있고 **분할·개수 집계와 함께 못 쓴다.** 한쪽이 켜지면 다른 쪽을
  잠그고 왜 잠겼는지 트리거에 적는다(`lockedReason`).

→ 오른쪽 축을 허용한 조건, 자동 채움 순서, `none` 접두사: [lessons: 매핑](docs/lessons.md#매핑)

## 미결정 사항

- **어드민 다중 파일** — 계약과 뷰어는 이미 파일 여러 개를 받는데 어드민만 하나다.
  `CanvasState` 하나 → 데이터셋 배열, 사이드바에 파일 목록, 차트마다 데이터셋 선택.
  **다음에 할 것.**
- **뷰어가 API에서 받는 것** — ⓐ 스펙 + 원본 행을 받아 뷰어가 `buildPlot()`을 직접 돌릴지,
  ⓑ 서버가 집계한 결과를 받을지. **ⓐ를 권한다** — 집계 규칙이 `aggregate.ts` 한 곳에만
  남는다. ⓑ면 서버가 정렬 규칙·`downsample`·분할 8개 상한·원형 "기타" 접기를 다시 구현해야
  하고, 어긋나는 순간 어드민에서 본 차트와 뷰어 차트가 달라진다(절대 원칙 1). 페이로드가
  실제로 문제가 되면 그때 ⓑ로 옮긴다 — 그건 서버 쪽 작업이라 이 레포는 안 건드린다.
- **큰 Excel의 시트 전환** — 지금은 원본을 다시 읽는다. 느려지면 워크북을 들고 있는 쪽으로.
- **상태관리** — 카드 배열·배치까지도 아직 `useState`로 충분. 다중 파일이 붙으면
  `App.tsx`가 데이터셋·차트·배치를 동시에 들게 되니 그때 재검토.
- **배치 세로 압축** — 지금은 켜져 있어 빈칸을 두지 못한다. `compactType`을 끄면
  자유 배치가 되지만 실수로 생긴 구멍도 남는다. 써보고 정한다.

## 남아 있는 lint 에러

`ui/badge.tsx`, `ui/button.tsx`, `ui/toggle.tsx`의 `react-refresh/only-export-components`
3건. shadcn CLI가 생성한 파일이고 컴포넌트와 `cva` variants를 함께 export하는 구조 때문이다.
생성 파일이라 손대지 않았다.

## 문서 관리

| 문서 | 담는 것 | 언제 읽나 |
|---|---|---|
| [CLAUDE.md](CLAUDE.md) | 지켜야 할 규칙 | 항상 (자동으로 실린다) |
| PROGRESS.md (여기) | 지금 참인 상태·구조·스펙 | 세션 시작할 때 |
| [docs/lessons.md](docs/lessons.md) | 근거·측정치·틀렸던 접근·함정 | 규칙을 바꾸려 할 때, "왜 이렇게 돼 있지?"일 때 |

**여기는 append-only가 아니다.** 새 발견이 이전 결론을 뒤집으면 **이전 문장을 지운다.**
틀린 근거가 남아 있으면 다음 세션이 그걸 믿고 코드를 쓴다 — 문서가 긴 것보다 이쪽이
위험하다. 겪은 것을 남기고 싶으면 `lessons.md`로 보내고 여기엔 한 줄 요약 + 링크만 둔다.
