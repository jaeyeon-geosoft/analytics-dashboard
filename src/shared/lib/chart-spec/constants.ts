/**
 * 한 화면에 올릴 수 있는 차트 수.
 *
 * 색 슬롯(8개)과는 무관하다. 카드가 좁아질수록 눈금 라벨과 범례가 먼저 무너지고,
 * 집계는 카드마다 따로 도는데 산점도는 한 장에 389ms를 쓴다.
 */
export const MAX_CHARTS = 4

/** 명세 id의 접두사. 세션 안에서만 겹치지 않으면 되고, 번호가 드러나 디버깅도 쉽다. */
export const CHART_ID_PREFIX = "chart-"
