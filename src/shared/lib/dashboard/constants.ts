/**
 * 계약이 바뀌면 올린다. 뷰어는 모르는 번호를 만나면 그리지 않고 그 사실을 말한다 —
 * 형식이 어긋난 채로 그리면 조용히 틀린 차트가 나온다.
 */
export const DASHBOARD_FORMAT = 1

/**
 * 그리드 규격. `layout`이 칸 단위라 **어드민과 뷰어가 같은 숫자를 봐야 한다** —
 * 한쪽만 바꾸면 같은 대시보드가 두 화면에서 다르게 배치된다(절대 원칙 1).
 * 바꾸면 이미 저장된 대시보드의 배치도 함께 움직이니 `DASHBOARD_FORMAT`을 올릴 것.
 */
export const GRID_COLS = 12
export const GRID_ROW_HEIGHT = 40
/** 카드 사이 간격(px). 어드민 캔버스의 `gap-3`과 같은 값이다. */
export const GRID_MARGIN = 12

/**
 * 카드가 이보다 작아지면 축·범례가 들어갈 자리가 없어 차트가 아니라 얼룩이 된다.
 * **양쪽이 같은 값을 봐야 한다** — 뷰어에서도 카드를 줄일 수 있게 되면서, 한쪽만
 * 작게 두면 어드민에서 못 만드는 크기가 뷰어에서 나온다.
 *
 * 세로 하한은 눈대중이 아니라 카드 내용의 높이에서 나온다 — 머리줄 약 55px(경고가
 * 두 줄이면 95px)에 플롯의 최소 높이 `min-h-64`(256px)를 더하면 351px이고,
 * 7칸이 7*40 + 6*12 = 352px다. 이보다 낮추면 플롯이 잘린다.
 *
 * 하한은 **저장되는 값이 아니라 계약 밖이다** — 계약은 x/y/w/h만 담는다.
 */
export const GRID_MIN_W = 3
export const GRID_MIN_H = 7
