/** 라벨 + 선택지 한 줄. 격자가 어긋나면 라벨과 Select의 짝이 안 보인다. */
export const ROW = "grid grid-cols-[5rem_minmax(0,1fr)] items-center gap-2"

/** 섹션 제목의 서체. 파일 목록·차트 종류·매핑이 같은 층으로 보여야 한다. */
export const SECTION_HEADING =
  "text-[10px] font-semibold tracking-[0.14em] text-muted-foreground uppercase"
